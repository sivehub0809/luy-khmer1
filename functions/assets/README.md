Place a Khmer-capable font file here before deploying Functions:

- `Battambang-Regular.ttf`

The receipt PDF generator will embed this font into the generated receipt HTML so Khmer text renders correctly in the exported PDF.
